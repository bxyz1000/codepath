// App-wide configuration settings
export const CONFIG = {
  // Production Vercel backend URL
  API_URL: 'https://codepath-mu.vercel.app',
  
  // Local development fallback (use your local IP for physical devices, e.g., 'http://192.168.1.X:3000')
  // API_URL: 'http://localhost:3000',
};
